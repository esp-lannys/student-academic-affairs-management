spring:
  application:
    name: auth-server
  cloud:
    config:
      discovery:
        enabled: true
        service-id: service-config-server

#secret key - should be encrypted
jwt:
  secret: ${JWT_KEY}
  #2 minutes validity
  expiration: 172800